"""Database wiring (SQLAlchemy 2.x, async).

This package provides the engine, session factory, and the declarative base for
future models. No ORM models are defined in the foundation sprint — only the
infrastructure that later sprints will build upon.
"""
