import 'package:flutter/material.dart';

/// Brand color tokens used to seed the application's light and dark schemes.
///
/// Centralised here so palette changes happen in one place.
abstract final class AppColors {
  /// Primary brand seed color from which the [ColorScheme] is derived.
  static const Color seed = Color(0xFF3D5AFE);
}
